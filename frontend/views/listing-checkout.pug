<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        script( src='/scripts/listing-checkout.js')
        title Listing Checkout
    body
        div.bookings-form
            form(action="", id='bookings-form')
                div.left
                    div.calendar
                        //- put calendar stuff in here?
                div.right

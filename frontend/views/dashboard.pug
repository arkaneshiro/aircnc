doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            //- include ../public/stylesheets/DashboardStyle/styles/style.css
        title Dashboard
    body
        div.navListings
            a(class="listingsButton" href="/listings") Listings
            a(class="logoutButton" href="/") Log out
        h1
        div.host-kitchens
        div.host-bookings
            div.bookings-button-container
                a(class="host-bookings-button" href="dashboard/bookings/") See all Bookings
            div.bookings-container
                div.pastBookings
                div.currentBookings

    script(src="/scripts/dashboard.js")

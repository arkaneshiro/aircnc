
document.addEventListener('DOMContentLoaded', async () => {
    //booking contains
    //startDate
    //endDate
    //isConfirmed ----
    //kitchenId ----
    //renterId ----
    //hostId ------

    //redirect if user is host? or not logged in or booking id DNE

    //get booking info from retrieved
    const bookingInfo = await fetch('http://localhost:8080/')

})
